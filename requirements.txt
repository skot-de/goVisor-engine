requests>=2.31
duckdb>=0.10
pyarrow>=15
pandas>=2.0
streamlit>=1.40
# Alt-Office in Vergabeunterlagen (govisor/docpipe.py: _doc_text/_xls_text).
olefile>=0.46
# ⚠ xlrd AB 2.0 KANN KEIN .xls MEHR (nur noch .xlsx). Wer die Grenze anhebt, schaltet
# 394 Dateien in 213 Vorgaengen still wieder ab, ohne dass irgendwo etwas rot wird.
xlrd<2
